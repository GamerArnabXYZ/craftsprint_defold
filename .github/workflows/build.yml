name: CraftSprint.io Build (WebGL & APK)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Download Defold bob.jar
        run: |
          mkdir -p tools
          curl -L -o tools/bob.jar https://github.com/defold/defold/releases/download/1.9.0/bob.jar

      - name: Build and Bundle WebGL
        run: |
          java -jar tools/bob.jar --root . -p js-web resolve distclean build bundle --bundle-output build/web

name: CraftSprint.io Build (WebGL & APK)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Download Defold bob.jar
        run: |
          mkdir -p tools
          curl -L -o tools/bob.jar https://github.com/defold/defold/releases/download/1.9.0/bob.jar

      - name: Resolve Dependencies
        run: java -jar tools/bob.jar --root . --archive --platform js-web --settings game.project resolve

      - name: Build WebGL Bundle
        run: java -jar tools/bob.jar --root . --archive --platform js-web --settings game.project build bundle --bundle-output build/web

      - name: Upload WebGL Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CraftSprint-WebGL
          path: build/web/
